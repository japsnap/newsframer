# Changelog — NewsFramer

The dated, git-tracked record of what ships. Newest first. Written at `/logout`.

## 2026-06-28 — Firecrawl · topic classes (VC) · double-gen lock + idempotent delivery · gateway watchdog · writer-on-subscription · reply-system foundation
_Big multi-day session. All shipped to the working tree; NOT committed (awaiting OK). Suite 44/44 throughout._

- **NF-B4 — Firecrawl scrape path for no-RSS sources (LIVE).** `agents/fetcher.fetch_web` now scrapes each no-RSS source's LISTING page via Firecrawl's clean-markdown API (parses `[title](url)` out of the markdown; strips images→alt; flattens multi-line titles), falling back to the old BeautifulSoup grab on ANY failure. Listing-page-only (1 call/source/run) → stays in the free tier. Config `firecrawl_enabled: true` (+ api_url/timeout/min_title/max_links). Live dry-run verified on the real TRT Global (60 articles); 2 bugs the live call exposed (CloudFront image URLs, polluted titles) fixed + test-locked. `tests/test_fetcher_firecrawl` (29). The 3 active scrape sources (TRT, Amnesty Evidence Lab, Delphi) now fetch content; CST still awaits its URL (NF-A4).

- **NF-NEW14 — per-topic appearance preference, shipped as a TOPIC-CLASS system (LIVE).** New `agents/topic_classes.py`: every source category belongs to a cadence CLASS (config `category_classes` + `topic_classes`) controlling 3 things — selection window, entry relevance bar, and min articles a cluster needs to be a theme. `regular` (daily news): 24h window, normal gate, **≥2 articles/theme** (a lone story → highlight, "option B": each topic still keeps its ONE best story). `low_frequency` (VC): **7-day window** (covers the twice-weekly cadence; dedup via the §4.3 deliveries set-difference), lower bar, single-article themes OK. Cleanly separates what overlapped: `user_context.weight` = "how relevant is this ARTICLE", the class = "how often does this CATEGORY appear". VC=low_frequency LIVE; verified on real data (today's brief unchanged; VC surfaces when it has content). `tests/test_topic_classes` (37). [Fixed the original mistake: VC's twice-weekly cadence judged on a 24h window.] UI exposure + WhatsApp shaping remain (fold into NF-NEW7 / NF-C1b).

- **Double-generation lock + idempotent delivery (cost + duplicate fix; satisfies/hardens NF-B5).** Diagnosed from execution_log/agent_runs: on 06-16/18/19 the build ran 2–3× a morning (each ran the analyst = 2–3× spend) — the OpenClaw cron command timed out ~15 min mid-build and OpenClaw re-invoked it. (A) `agents/gen_lock.py` — a per-slot+JST-day file lock in `run_brief.py`; a concurrent/retry build NO-OPS. (B) idempotent delivery in `deliver_brief.py` — skip the send if today's brief was already delivered to the surface (a local marker written after a confirmed send, before the DB record, closes the kill-before-record window). Config `run_lock_enabled` / `run_lock_stale_minutes` / `delivery_idempotent` (on, reversible). Verified: simulated double-run generated once + `already_delivered` true/false on real data. `tests/test_gen_lock` (8) + `test_delivery_idempotent` (11). Fix C (cron timeout): confirmed already adequate (no doubles since 06-19) + the lock guarantees it regardless; no change.

- **Independent gateway watchdog (LIVE — fixes the silent-outage blind spot).** 2026-06-25 incident: the gateway crashed after the 06:00 run + wasn't auto-restarted, so the 11:00 WhatsApp cron never fired — and the §4.5 watchdog, being a gateway cron itself, couldn't alert (~2-day silent WhatsApp outage). New `gateway_watchdog.py` + a Windows Scheduled Task (`register_gateway_watchdog.ps1`, registered elevated) running every 20 min OUTSIDE the gateway: pings it, and if down restarts it + alerts Telegram (missed crons catch up on reconnect). Config `gateway_watchdog_enabled` etc. Verified live (task Ready, LastTaskResult 0). The outage was resolved this session (gateway restarted → WhatsApp catch-up delivered to both chats, +300 records).

- **Writer on the flat Max subscription (LIVE).** `agents/cc_writer.py` + a refactored `writer.run_writer` model seam: the writer's draft runs via headless Claude Code (`claude -p`, haiku) on the flat subscription instead of the metered API — subscription-first, **API fallback on any failure** (never drops), `model_used` logs the path, `cost_usd=0` on the subscription. Load-bearing fix: `MAX_THINKING_TOKENS=0` (Claude Code's default extended thinking hung the big synthesis >600s; off → ~70–107s) + feed the large prompt via temp-file→stdin redirect (Python `input=` deadlocks the node CLI on Windows with a large prompt). Config `writer_use_subscription: true` + `writer_subscription_model: haiku` + `writer_subscription_max_thinking_tokens: 0`. Verified end-to-end: full brief in 107s, $0 metered; content/format unchanged. Analyst stays on the API (queued: NF-ANALYST-BATCH → NF-ANALYST-SUB).

- **Phase 2 reply-system FOUNDATION (built, NOT wired live).** The deterministic safety rails + the LLM-agent brief for the "ask the administrator" reply system: `agents/reply_router.py` (per-chat 20/day cap, conversation-turn cap, owner-only-write gate, language routing, input-truncation token guard), `agents/reply_persona.py` (content-scope system prompt: general/news/within-group only — never the operator or other subscribers; + operator-mention hard-guard), `agents/signup_flow.py` (one-message signup elicitation + pure parsers to pre-fill the future dashboard). Config `config/chat_interaction.example.yaml`. `tests/test_reply_router` (19) + `test_reply_persona` (11) + `test_signup_flow` (17). The LIVE OpenClaw-inbound wiring + Haiku responder is the next step (NF-NEW5 / NF-E1) — OpenClaw does the conversation, the Python is only the rails.

- **WhatsApp multi-language → ONE message (fix).** A chat configured with 2 languages (Happy Rana = EN+UR) was sent the brief as TWO separate messages, one per language — flooding the group ("populating the chat"). `run_whatsapp_brief` now joins a chat's languages into a SINGLE message (new pure `combine_messages`; config `whatsapp_combine_languages: true` + `whatsapp_language_separator`; default on, reversible → `false` restores per-language). Muda (EN-only) unchanged. `tests/test_whatsapp_format` +3 (16). Applies on the next 11:00 dispatch.

- **Ops / diagnoses (no code):** Gemini's recurring "primary unreachable" is **Google-side 503 overload, not the VPN** (DNS/TCP to the API clean, VPN off). §4.1 carry-forward: per Shota, **keep the current build** — the cost goal is already met (idempotent classify/analyse + the 11:00 gap-cap); the literal carried-delta would save ~$0 + risk the live path. Dropped item: none.

## 2026-06-24 — World Cup result-window fix (NF-F5)
- **NF-F5 — an in-progress match no longer vanishes between the daily runs (the easy way, config-only).** `worldcup_result_window_hours` 24→28 in `config/models.yaml` (one line, no code). Derivation: 28 = the ~24h gap between the once-daily 11:00 WC messages + ~4h match-length slack, so a match still IN PROGRESS at one day's run is caught as a FINISHED result at the next day's run instead of falling through the live→results gap. Accepted trade-off (deliberate — throwaway WC feature, deletion scheduled ~07-21): a match finishing just before a run may show in results two days running; the clean carry-forward (report-once via a small state file) was NOT built per Shota ("don't over-build it"). WC tests green (44+53), full suite **35/35**; applies on the next 11:00 run, revert = set back to 24. Also backlogged the idea of extracting WC into a reusable **seasonal-tournament template** instead of deleting it.

## 2026-06-23 (RC session — de-hardcode → config)
- **Q1 — Investigations section header → config (no-hardcoding rule).** `agents/drop_reports.py` `INVESTIGATIONS_HEADER` now reads `investigations_section_header` from `config/models.yaml` via a wrapped module-level load (falls back to the literal `"## 🔍 Investigations"` on a missing/broken config, so import can't break). Default reproduces today's brief byte-for-byte; full suite **35/35** green; confirmed the value is genuinely config-sourced (not a silent fallback).
- **Q2 — "What Changed" section header → config (no-hardcoding rule).** `agents/thread_tracker.py` `WHAT_CHANGED_HEADING` now reads `what_changed_section_header` from `config/models.yaml` (wrapped module-level load, reading the yaml directly to avoid a circular import with `writer`; falls back to `"## 📈 What Changed"`). Default reproduces today's brief byte-for-byte; full suite **35/35** green; confirmed config-sourced.
- **Q3 — one-sided-coverage warning strings → config (no-hardcoding rule).** The "_⚠ Left/Right-leaning media only — opposing view absent._" notes baked into the writer's prompt instruction now come from `coverage_warning_left` / `coverage_warning_right` in `config/models.yaml` (two `agents/writer.py` module constants, injected into the same f-string). Default reproduces the prompt byte-for-byte; full suite **35/35** green; confirmed config-sourced with content intact.
- **Q4 — quiet-day notice text → config (no-hardcoding rule).** The `"_Quiet news day — fewer items than usual._"` notice prepended to a thin-day brief now reads `quiet_day_text` from config in BOTH paths — `agents/writer.py` (module constant `QUIET_DAY_TEXT`) and `run_whatsapp_brief.py` (inline `load_config().get(...)`). Default reproduces today's output; full suite **35/35** green; both paths confirmed reading the one key.
- **Q5 — WhatsApp language display labels → config (no-hardcoding rule).** `run_whatsapp_brief.py` `LANG_LABELS` (`{ur: "Urdu (Urdu script)", ar, hi, en}`) is now a wrapped config read of `language_labels` from `config/models.yaml`, default = the current dict. Full suite **35/35** green; the live dict confirmed identical to the original.
- **Q6 — WhatsApp markdown markers → config (no-hardcoding rule).** `run_whatsapp_brief.md_to_whatsapp` now reads the bullet (`"• "`) and bold/heading (`"*"`) markers from `whatsapp_bullet_marker` / `whatsapp_emphasis_marker` (lambda regex replacements so a custom marker can't break the substitution). Default reproduces the formatting; full suite **35/35** green (incl. `test_whatsapp_format`); heading/bold/bullet round-trips identical.
- **Q7 — WhatsApp translation system prompt → config (no-hardcoding rule).** `run_whatsapp_brief.translate`'s inline English translator prompt now reads `whatsapp_translate_system_prompt` from config (a template with a `{label}` placeholder for the target language, filled via `.replace`; in-code default = the original). Default reproduces the instruction; full suite **35/35** green; the template + `{label}` fill verified.
- **Q8 — gateway send timeout → config (no-hardcoding rule).** `agents/deliver.gateway_send`'s `timeout=120` now comes from `gateway_send_timeout_seconds` via a module constant (matching `TELEGRAM_LIMIT`). Full suite **35/35** green; the live param default verified still 120.
- **Q9 — Telegram alert timeout → config (no-hardcoding rule).** `agents/deliver.send_alert`'s `requests.post(..., timeout=20)` now reads `telegram_alert_timeout_seconds` via a module constant. Full suite **35/35** green; constant verified 20.
- **Q10 — drop-report reply trigger prefix → config (no-hardcoding rule).** `get_drop_report.normalize_slug`'s `"more:"` reply prefix now reads `drop_report_reply_trigger` (wrapped module-level config load, same pattern as the section headers). Full suite **35/35** green; strip behaviour (incl. case-insensitive) verified intact.
- **Q11 — drop-report slug limits → config (no-hardcoding rule).** `agents/drop_reports.py` `_SLUG_MAX=40` / `_SLUG_WORDS=4` now read `drop_slug_max_chars` / `drop_slug_max_words` from `_CFG`. Full suite **35/35** green (incl. `test_drop_reports` slug-gen); constants verified 40/4.
- **Q12 — analyst breaking-news keywords → config (no-hardcoding rule).** `agents/analyst.validate_and_clean`'s inline `("breaking", "just in", "alert", "urgent", "live:")` actionability-bump list now reads `analyst_breaking_keywords` via module constant `BREAKING_SIGNALS`. Full suite **35/35** green; tuple verified identical.
- **Q13 — analyst actionability/perspective thresholds → config (no-hardcoding rule).** The three inline relevance gates in `validate_and_clean` (rel≥8 IMMEDIATE→act2, rel≥7 breaking→act2, rel≥7 perspective) now read `analyst_actionability_rel_threshold` / `analyst_breaking_actionability_rel_threshold` / `analyst_perspective_rel_threshold` via module constants. Full suite **35/35** green; constants verified 8/7/7.
- **Q14 — analyst topic caps → config (no-hardcoding rule).** `validate_and_clean`'s `topic[:40]` char cap and `[:8]` topic cap now read `analyst_topic_max_chars` / `analyst_max_topics` via module constants `TOPIC_MAX_CHARS` / `MAX_TOPICS`. Full suite **35/35** green; constants verified 40/8.
- **Q15 — fetcher PR/sponsored keywords → config (no-hardcoding rule).** `agents/fetcher.is_pr_article`'s inline `["sponsored", "partner", "press release", "advertorial", "paid post"]` now reads `fetcher_pr_keywords` (new wrapped module-level `_CFG` + `PR_KEYWORDS`, for the helpers that don't receive `config`). Full suite **35/35** green (incl. `test_fetcher_filters`); list verified identical.
- **Q16 — fetcher scrape HTTP timeout → config (no-hardcoding rule).** `agents/fetcher.fetch_web`'s `requests.get(..., timeout=10)` now reads `fetcher_scrape_timeout_seconds` via module constant `SCRAPE_TIMEOUT`. Full suite **35/35** green; constant verified 10.
- **Q17 — inline LLM system prompts → config (no-hardcoding rule).** The investigative drop-summary prompt (`writer.DROP_SUMMARY_SYSTEM`) and the sequencing structured-fact extraction prompt (`thread_tracker.EXTRACT_SYSTEM`) now read `drop_summary_system_prompt` / `sequencing_extract_system_prompt` from config (literal block scalars; the exact original kept as the in-code default). Full suite **35/35** green; both prompts verified **byte-for-byte** identical to the originals.
- **Q18 — operator timezone offset → config (no-hardcoding rule).** Every `JST = timezone(timedelta(hours=9))` now reads `operator_tz_offset_hours` (default 9). Scope was WIDER than the backlog's 5-file note — the literal was actually defined in **6** files (`writer`, `thread_tracker`, `fetcher`, `cost_report`, `deliver_brief`, `check_run_health`); `run_whatsapp_brief.py` *imports* JST from `writer` (covered for free), and `fetcher`/`cost_report` were missing from the backlog list. The World Cup files were left untouched (excluded from de-hardcoding; WC deletion scheduled 07-21). In the three `_CFG` files `JST` was relocated to just after `_CFG` loads (it was previously defined before the config was available). Full suite **35/35** green; every module's `JST` verified still **UTC+9**.
- **Q19 — brief section ordering → config (no-hardcoding rule).** `writer.run_writer`'s three fixed post-pass splices (Investigations → What Changed → Blindspot) were refactored into named closures dispatched in `brief_section_order` order (default = the current sequence, so byte-for-byte identical). Full suite **35/35** green; default order verified.
- **Q20 — deduplicator price-event keywords → config (no-hardcoding rule).** `agents/deduplicator.PRICE_EVENT_KEYWORDS` (the price/event term list that resolves a cluster to `price_event`) now reads `deduplicator_price_event_keywords` from `_CFG`. Full suite **35/35** green (incl. `test_deduplicator`); the config list and the live constant verified identical to the original.
- **Q21 — fetcher junk-length thresholds → config (no-hardcoding rule).** `agents/fetcher.is_junk_url`'s `len(url) < 30 or len(title) < 15` now reads `fetcher_min_url_length` / `fetcher_min_title_length` via module constants. Full suite **35/35** green (incl. `test_fetcher_filters`); constants verified 30/15.
- **Q22 — source-skew bars → config (no-hardcoding rule).** `skew_warning`'s `min_sources=3` / `skew_ratio=0.75` are now passed from `source_skew_min_sources` / `source_skew_ratio` at the live call site in `writer.run_writer` — `agents/source_skew.py` stays PURE (the signature defaults remain as the fallback). Full suite **35/35** green (incl. `test_source_skew`); values verified 3/0.75.
- **Q23 — critic labels → config (no-hardcoding rule).** `agents/critic.py`'s severity labels (`Critical`/`Important`/`Minor`) and non-theme section names (`highlights`/`investigations`) now read `critic_severity_labels` / `critic_non_theme_sections` via a wrapped module-level config load (the severity list is length-guarded → falls back to the defaults if not exactly 3). Full suite **35/35** green (incl. `test_critic`); labels + non-theme tuple verified identical.
- **Q24 — cost-report task labels → config (no-hardcoding rule).** `agents/cost_report._TASK_LABELS` (`{brief: "📲 Telegram brief", whatsapp_brief: "💬 WhatsApp"}`) now reads `cost_report_task_labels`; cost_report's config access was refactored to a shared `_CFG` (replacing Q18's standalone `_tz_offset`, so `JST` and the labels share one load). Full suite **35/35** green (incl. `test_cost_report`); labels verified identical and `JST` still UTC+9.
- **Q25 — execution-log project name → config (no-hardcoding rule).** `run_log.build_exec_row`'s `project="newsframer"` is now passed from `project_name` (new wrapped `_project_name()` helper, sibling of `_exec_log_enabled()`) at the `mirror_execution_log` call site; `build_exec_row` stays pure (its default = the fallback). Full suite **35/35** green (incl. `test_run_log`); resolves to `newsframer`.
- **Q26 — Blindspot base URL → config (no-hardcoding rule).** `agents/blindspot.BASE_URL` (`"https://ground.news"`) now reads `blindspot_base_url` via a wrapped module-level config load, and `DEFAULT_URL` is now *derived* (`BASE_URL + "/blindspot"`), killing the second hardcoded domain. Full suite **35/35** green (incl. `test_blindspot`); both URLs verified identical at the default.
- **NF-NEW2 — verified RESOLVED on real data (no code change).** A read-only audit of 7 days of real WhatsApp deliveries: the newest delivered story was ~0.1–0.2h old on 6 of 7 days (2.1h on 06-19), with **3–17** fresh-in-gap (≤5h) stories delivered per chat per day, against **99–183** fresh-published stories/day of supply. The 06-15 symptom ("newest delivered = 5h, nothing newer than the 06:00 fetch") no longer reproduces — already fixed by the 06-18 gap-fetch (`b92edb2`) plus the existing fresh-at-floor admission. Closed. The naive "admit all fresh" fix was deliberately **not** shipped (it would have re-leaked off-topic / sub-floor items, undoing the crypto-topic-exclude fix). 06-22 had no delivery (the Gemini/VPN outage).
- **Also this RC leg (no code):** spec frontmatter `version` 0.2→0.3 (one-time operator-authorized edit of the read-only spec, aligning the field with its existing v0.3 changelog); Google TAG source → leave inactive (dead RSS, no DB change); NF-F5 (World Cup live↔result gap) deferred — not a clean S (the window-widen regresses to double-reports, the clean fix needs cross-run state), superseded by the 07-21 WC deletion.

## 2026-06-22 — output fixes · LLM-outage resilience · body-based length rebuild · per-surface config
- **Output fixes (work order 2026-06-19).** Per-surface link/source display: **Telegram** renders a clickable SOURCE NAME with NO visible URL (gateway HTML parse mode; the old brief leaked "Source (URL)" via a plain-text fallback), **WhatsApp** renders TITLE + SOURCE only (was literal `[Dawn](url)`). **TG highlight dedup** collapses same-event (deduplicator `cluster_id`) and identical-topic-set highlights (today's 4 crypto-regulation highlights → 1). **Football OFF** (`football_enabled: false`; World Cup kept). **WhatsApp crypto-leak fixed** — root cause was the topic filter matching crypto/markets stories tagged `geopolitics affecting markets`; added a per-category topic **EXCLUDE** filter (`whatsapp_topic_exclude_keywords`, "loose" default), so crypto/markets drop from geo/pak while crypto-MALWARE still reaches the cyber surface. **World Cup parse fixes** — the host marker strip now handles `(H, Q)` (Mexico→Group A, killing the phantom "Group ?"), and a footnote-letter points cell (`"1 a"`) no longer throws and drops tied teams (Groups G/H were empty) → all 12 groups × 4 teams parse. (`f4e2ba2`)
- **LLM-outage resilience (incident 2026-06-22).** Gemini became unreachable from the host (root cause: a local **VPN block** to Google's API IPs — DNS fine, Anthropic fine, Gemini globally up); the Gemini-only stages had no request timeout/fallback and HUNG the whole pipeline (classifier thrashed 63 min, analyst scored 0, no Telegram brief; the WhatsApp run hung). New `agents/llm_client.py` — a hard wall-clock timeout (daemon-thread bound; litellm's own `timeout` did NOT bound a connect hang), an Anthropic fallback, and a sticky per-run breaker. Wired across **classifier / analyst / title_dedup** (timeout + fallback), **deduplicator embeddings** (bounded + graceful skip), and **sequencing** (bounded + abort). Config-driven (`llm_request_timeout_seconds`, `llm_breaker_threshold`, `*_fallback_model`); the fallback only fires on a primary FAILURE, so a healthy run is unchanged. Verified live against the real outage (fail-fast → Anthropic → breaker). `tests/test_llm_client` (22). (`f4e2ba2`)
- **Length model rebuilt → BODY-based (items 1-3).** Per-theme SIZE is now ONE control. The real driver was the prompt's paragraph guidance (the char cap was only a ceiling the LLM underfilled, so L did nothing = the bug). `theme_size_levels` are absolute per-theme **BODY** char targets (S=800 / M=1600 / L=3200, EXCLUDING the `**Articles:**` source list), injected as a scaled paragraph count; the source list is a SEPARATE cap allowance (`writer_per_theme_source_chars`), so SIZE depends on the body only. Total cap DERIVED (`(body+source)×#themes + highlights`); output-token budget derived too. Legacy per-chat length consolidated short=S / medium=M / long=L. Verified on rendered BODY: M~1320/theme (= today's body), L~2270 (~1.66×) — a clean 2.0× isn't reachable on Haiku via whole paragraphs (4 paras=1.66×, 5=2.64×); L=3200 (4 paras) chosen as closest with M=today. (`f4e2ba2`, `465cfee`)
- **Per-surface config (items 4-7), all UI-ready, defaults preserve behaviour.** TG **content scope** (`surfaces.telegram.categories`, default `["*"]` = all bundles). **Three fetch tiers** `fetch_profiles` full=50 / normal=30 / light=10 — **LIVE = normal (30/source, down from 100)** — and the fetcher now **sorts newest-first before capping** (was feed-order; "newest N" wasn't guaranteed). **WhatsApp per-category theme cap = 2** (mirrors the Telegram bundle cap via `cluster_bundle`). **Highlights min relevance 8 → 5.** **Secondary-language source toggle** `include_sources_in_secondary_language` (default false = current). (`f4e2ba2`)
- **Ops:** full suite **35 files green** (+`test_surface_render`, +`test_llm_client`; fetcher/length/worldcup updated); secret/PII scan clean; `.env*` / WhatsApp registry / BACKLOG / CLAUDE / AGENTS confirmed gitignored. Pushed `626fbad..465cfee` to origin/main. Also this session (not code): per-chat settings audit (research), a Phase-1.5/2 status review, and **27 de-hardcode `#rc` items + the 2026-07-21 WC/football deletion** queued in BACKLOG.

## 2026-06-18 (cont.) — incremental gap-fetch · WhatsApp cost tracking · daily cost report
- **NF-4 (§4.1) — the 11:00 slot fetches only the gap, not the full day.** The gap-refresh re-pulled each source's full ~24h window and discarded the dupes (free, but a wasteful re-download). Now `run_whatsapp_brief.gap_refresh` sets `NEWSFRAMER_MAX_FETCH_WINDOW_HOURS` and `fetcher.source_window_hours` caps the per-source window to it — config `whatsapp_gap_fetch_hours: 5` (the real 06:00→11:00 gap; `0` = disable the cap = full window, NOT zero hours). The 06:00 `run_brief` is a separate process ⇒ unchanged; classify/analyse were already incremental; the brief still reads the full 24h pool. `tests/test_fetcher_filters` +4. (`b92edb2`)
- **NF-14 — WhatsApp-path LLM cost now tracked (group + Muda).** The WhatsApp brief generation + Urdu translation were invisible; now logged to `agent_runs` + `execution_log` via `record_run` (`whatsapp_writer` / `whatsapp_translate`) under a per-run trace_id (task_type `whatsapp_brief`). De-hardcoded `whatsapp_send_timeout_seconds` + `whatsapp_gap_stage_timeout_seconds`. `tests/test_whatsapp_cost` +10. (`1c526ca`)
- **NF-14 — daily cost report to the operator's Telegram.** New `agents/cost_report.py`: after the 11:00 WhatsApp brief reaches the group + Muda, the operator gets a Telegram rollup of the day's spend (Telegram brief + every WhatsApp chat, by stage, with % of the $2 cap). The chat list is read from the registry — **dynamic, never hard-coded** to a fixed number of groups. Config `cost_report_enabled` (on) + `cost_report_cap_usd`; fully isolated. Live render verified ($0.37, 19% of cap). First Telegram-vs-WhatsApp split lands on the next full 11:00 run. `tests/test_cost_report` +17. (`03252d0`)
- **Firecrawl decision (backlog NF-B4, not yet built).** For the §3.3 scrape path, decided on **Firecrawl** over the OpenClaw browser (verified via docs — more autonomous; the browser stops for a human at login/2FA). Free tier (1,000 scrapes/mo) covers our 3–4 scrape sources at ~15% if kept **listing-only**; needs the operator's free `FIRECRAWL_API_KEY`. Recorded in BACKLOG; not shipped.
- **Reconciliation:** verified that **NF-A3 (World Cup)** is fully built + live (backlog said "half built"), and **NF-A2 (football)** + **NF-D2 (blindspot)** ship in the entry below; their backlog items are now closed. Full suite **33/33** green; everything pushed to GitHub (`b92edb2..03252d0`).

## 2026-06-18 (Phase 1.5 build-out — crypto materiality · football · blindspot · critic · length levels · exec-log)
- **NF-C1a — per-category materiality + price-aware sequencing notes.** Crypto/price deltas were noisy (a ~$1k BTC wiggle cleared the abs:1 bar). Added config-driven `sequencing_materiality_overrides` (any source category may override the change-bars; crypto seeded `magnitude_pct: 0.10`, `magnitude_abs: 0` = percentage-only ⇒ a price must move ≥10%) + `sequencing_price_units`; `is_material_numeric` now treats `abs ≤ 0` as "disable the absolute bar". New price format `Bitcoin: was $60K on Jun 16, now $66K (+10%)` (humanized K/M + signed %); non-price notes keep the arrow. `resolve_materiality` + `brief_stories(.., categories_map)` carry per-story category (writer untouched). **Sequencing flipped ON** (`sequencing_enabled: true`). Proven read-only on real data — the noisy BTC delta is gated out. (`941749a`)
- **NF-A2 — Football news (WhatsApp), and a reusable event-feed TEMPLATE.** New `agents/event_feed.py` (topic-agnostic RSS-headline digest) + `run_football_brief.py`; appended to the 11:00 WhatsApp dispatch via `maybe_send_football`, fully isolated (no LLM, no DB — can't affect the main brief). Config `football_*`. **LIVE** (`football_enabled: true`). The module is deliberately generic so a future event = a config block + one `maybe_send_*()` call. (`941749a`)
- **NF-D2 — Blindspot of the day (Telegram + WhatsApp).** New `agents/blindspot.py` scrapes Ground News' Blindspot page (no API/RSS), picks the day's strongest under-covered story with a one-line "which side is ignoring it" note; spliced into the Telegram brief AND the 11:00 WhatsApp dispatch, per-channel config toggles, brittle-but-graceful (a parse miss just skips the line). Config `blindspot_*`. **LIVE** both channels (`blindspot_enabled: true`). (`941749a`)
- **NF-F1 — Critic wired (report-only, never blocks).** The pre-send Critic now runs in `deliver_brief.py`: checks structure + citations on the finished Telegram brief and pings *your* Telegram only on a real Critical/Important problem; it NEVER patches or blocks the send (fully wrapped, isolated). Config `critic_enabled` / `critic_alert_min_severity`. **LIVE** (`critic_enabled: true`). (`941749a`)
- **NF-E2 — Length levels (per WhatsApp chat).** Each chat can be `short` / `medium` / `long` via a registry `length` field; `resolve_length` overrides the brief-shape knobs, with `medium` == prior defaults (so a chat with no length is byte-for-byte unchanged). Config `length_levels` + `default_length`. (`941749a`)
- **SETUP_PROMPT.md** — a paste-into-AI guided setup for non-developers; it delegates specifics to the real repo files so it can't drift from reality. (`941749a`)
- **Configurable analyst topic-weight (NF-NEW14 groundwork).** The `user_context.weight` → relevance-points SCALE was hard-coded in the analyst prompt; moved to config (`analyst_weight_strong_points` / `_mild_points`), default reproduces the prompt byte-for-byte (test-pinned). Weight *values* stay in Supabase; the *scale* is now tunable + UI-ready. (`941749a`)
- **NF-14 — execution_log (per-run trace + cost observability, spec §9).** New Supabase `execution_log` table (migration `create_execution_log_nf14`): one row per engine per run, all sharing a `trace_id` minted by `run_brief.py` (via `NEWSFRAMER_TRACE_ID`). `agents/run_log.record_run` mirrors each `agent_runs` row into it, best-effort and fully isolated (never in the critical path). The deduplicator was routed through `record_run`; the writer sets `artifact_verified` when the briefings row exists. Config `exec_log_enabled` (default true). `execution_log.sql` kept for reproducibility; live round-trip verified + cleaned up. (`3737153`)
- **NF-F4 confirmed closed (investigation, no code change).** The wrong/thin-brief bug: traced on live data — the stray UTC-dated brief stopped firing after 06-13, and delivery already excludes it (`brief_select.pick_best_brief`). The only residual is decommissioning the dormant GCP Cloud Run job (operator action).
- **Phase 1.5:** of the 15 items, all done bar item 3 (Firecrawl scrape — needs an API key) and item 4 (carry-forward — optional optimization); item 7 (abuse controls) deferred to Phase 2 (the bot is one-way today). Cost discipline holds: tracked spend ~$0.06–0.27/day, well under the $2 cap. Tests: +`test_event_feed`, +`test_blindspot`, +`test_length_levels`, +`test_analyst_weight`; `test_critic` / `test_thread_tracker` / `test_run_log` updated. Full suite **31/31** green.

## 2026-06-17 (NF-C1 — news-context-shift sequencing, wired OFF)
- **NF-C1 (spec §4.4) — news-context-shift sequencing built, WIRED OFF by default.** When an ongoing story's tracked FACT changes day-over-day, the brief gains one concise, sourced "what changed" note — data-to-data per §4.3 (every delta carries old_ids AND new_ids; never diffs prose or a prior brief), only past a per-type materiality bar. New `agents/thread_tracker.py`: Stage 0 embedding-match today's brief stories to prior threads (`cluster_id` = the 48h spine; the stored embedding bridges the 7-day context), Stage A cheap-model structured-fact extraction, `classify_delta` type-family-consistent diff across the 7 delta types (magnitude / cumulative / status / reversal / scope / attribution / forecast). Persists to a new Supabase **`tracked_threads`** table (migration `create_tracked_threads_nf_c1`; `tracked_threads.sql` kept for reproducibility; service-role round-trip verified); **Monday-06:00-JST weekly hard reset (§4.2)** owned by the module. Renders BOTH a `## 📈 What Changed` subsection (deterministic splice) AND inline-in-theme (LLM weaves the verified note, figures preserved); placement is config (`sequencing_placement`). Wired into `writer.run_writer` gated by `sequencing_enabled` (**default false ⇒ brief byte-for-byte unchanged**, verified: `build_user_prompt` identical off vs on). Telegram brief only. `tests/test_thread_tracker.py` (62 checks); suite 27/27. Read-only `--simulate` over the last 7 real briefs produced clean status/scope deltas with backing article IDs, zero prose-derived. **To enable:** set `sequencing_enabled: true` (changes the live Telegram brief — do with a watched first run). Follow-ups backlogged: NF-C1b (WhatsApp), NF-C1c (settings placement toggle). (`5b93fdf`)

## 2026-06-16 (citations · fetch profile · provable 24h window)
- **NF-NEW1 — hyperlinked source names, no bare URLs.** Theme citations now read as a clickable SOURCE NAME (`[N] <title> — [<source name>](<url>)`) instead of a hyperlinked title with a plain-text source; added a global format rule forbidding any raw URL. Deterministic safety net `agents/link_monitor.py` (`bare_url_flag`) scans the finished brief and logs a greppable `⚠ BARE URL` flag — log-only, never alters/blocks the brief (NF-F2 char-monitor shape). `tests/test_link_monitor.py` (19). Telegram scope; the WhatsApp link-handling decision stays open (NF-NEW4/E-series). (`34f22fe`)
- **NF-NEW11 — switchable full vs light fetch profile (config-only).** One key (`fetch_profile: full|light`) toggles today's 100/source full-day context vs the cheaper most-recent-~10 profile, no code edit. `fetcher.resolve_fetch_caps` (pure); a missing/unknown/None profile falls back to `full` with the old inline defaults ⇒ hollow config reproduces current behaviour (verified live `('full',100,3000)`). `tests/test_fetcher_profile.py` (19). (`f5c4ae1`)
- **NF-NEW2 — each session provably spans a true 24h window.** Traced end-to-end: both daily sessions load `published_at >= now - writer_window_hours(24)` (true 24h by publication time, not created_at); the 11:00 WhatsApp run loads the FULL 24h (existing ~19h + the 06:00→11:00 gap), not just the gap. New `agents/window_audit.py` (pure span report) logged in BOTH paths so every run PROVES its reach (cutoff, count, oldest, newest, fresh<6h). Live DB evidence: WhatsApp deliveries reach oldest ~20.8–23.9h; a read-only span check showed geo/pak oldest = 24.0h. `tests/test_window_audit.py` (21). The window is sound; the one residual gap (the 11:00 FRESH 06:00→11:00 slice intermittently not surfacing) stays open in BACKLOG. (`21daaff`)

## 2026-06-15
- **Fix (NF-NEW3): Investigations section no longer monopolised by one source.** That morning's 06:00 brief showed 3 Investigations items all from Middle East Eye (its high-volume live-blog). Added a per-source cap to the drop picker — `agents/drop_reports.pick_diverse` + `agents/writer.build_drops`, config `drop_report_max_per_source: 1` — so each Investigations slot comes from a different source (backfills only when too few distinct sources exist). `tests/test_drop_reports.py` +4 cases (32 checks); suite 17/17. The deeper root — Middle East Eye is bundled as `investigative` but is a news outlet — is teed up in `recategorize_mee.sql` (a live-DB move to `geopolitics`, to run at home).

## 2026-06-14 (RC session)
- **NF-B1 verified live** — the `## 🔍 Investigations` drop-report section renders in a *delivered* brief (today's `dd781149`, sent to Telegram with 47 articles recorded). Closes NF-B1.
- **No-hardcoding enforcement test** — new `tests/test_no_hardcoding.py` fails the suite if any LLM call passes a bare `temperature`/`max_tokens`/… literal instead of a `config.get(...)` value (the most common no-hardcoding regression); includes a negative self-test so the guard can't pass hollow. Current engine code is clean.
- **dispatcher.py marked DEPRECATED + literals tidied** — added a clear DEPRECATED banner (superseded by `agents/deliver.py` + `deliver_brief.py`) and routed its HTTP timeout and inter-message pause through `config.get(key, default)`. Reference-only file; the daily crons never run it.
- **NF-E5: WhatsApp geopolitics topic filter broadened** — added rights / authoritarianism / trafficking / refugee / occupation / coup terms to the geopolitics keyword list (config-driven: live registry + committed `*.example.yaml` template + code defaults) so genuinely geopolitical stories whose analyst topics use that language are no longer dropped. The documented false-negative in `tests/test_topic_match.py` is flipped to a kept assertion.
- **Critic v1 (§10.13 / NF-F1) — standalone, not yet wired** — new `agents/critic.py` reviews a finished brief and reports issues by severity (Critical / Important / Minor): empty brief, missing citations (anti-hallucination), empty sections, character overrun vs the theme-scaled cap, thin theme count — and **never patches**. Built unwired so it cannot affect delivery until deliberately switched on between Writer and delivery. `tests/test_critic.py` (16 checks, incl. a clean-brief-passes and a never-mutates-input test). A read-only `run_critic.py` runs it over the latest stored brief for manual inspection — no send, no record.
- **World Cup message formatter (NF-A3) — built, unwired** — new `agents/worldcup_format.py` turns structured match data into the WhatsApp World Cup message: flag emojis + results with **goalscorers + minute**, per-group standings tables (monospace), next-24h fixtures, and an optional written wrap-up. Pure/deterministic (no API key, no network, no LLM) — the API-Football fetch + 11:00 cron wire in later. `tests/test_worldcup_format.py` (30 checks). Data source decided: **API-Football (api-sports.io), free** — verified legitimate (Scamadviser + developer reviews).

## 2026-06-13 (NF-F2 — char-overrun monitor)
- A brief that exceeds its theme-scaled character cap now **flags in the run log** (`⚠ CHAR OVERRUN: <n> chars > cap <c> (+Δ, +pct%)`) instead of passing silently — surfaces the ~15–17% editorial drift noted in spec §15. New pure helper `agents/char_monitor.py` with a config-gated tolerance (`writer_char_overrun_warn_ratio`, default `1.0` = flag any overrun; e.g. `1.15` = only flag >15% over — no hard-coding), wired into **both** brief generators: the Telegram writer (`agents/writer.py`) and the WhatsApp brief (`run_whatsapp_brief.py`, which previously logged no char count at all).
- Deliberately a **quality** flag, not a run-health failure: it does **not** touch `agent_runs.status`, so the §4.5 watchdog still treats an over-cap-but-successful brief as success (no schema change, no daily false alarms).
- Tests: `tests/test_char_monitor.py` (18 checks — over-cap flags, within-cap/tolerance silent, bad inputs never crash); full repo suite green, no regressions.

## 2026-06-12 (docs system restructure)
- Adopted the standard doc layout. **CLAUDE.md = orientation only**: the login/logout ritual prose was removed (now mechanized via the global SessionStart/SessionEnd hooks + `/login` `/logout` commands); the engine reference is pulled in via `@AGENTS.md`; the spec stays central at `aiautomation\spec\NewsFramer_Spec.md` (read-only).
- **BACKLOG.md** converted to the standard schema (`## Guardrails (NEVER-AUTO)` + `## Items` with `#rc`/`#ask`/`#idea` tags, size, acceptance check).
- **AGENTS.md** is now gitignored/local (privacy convention: only `CHANGELOG.md` + `README.md` are tracked; `CLAUDE.md`/`BACKLOG.md` were already local).
- Ecosystem Blueprint reference clarified as living in Obsidian; VISION pointer added.
- This `CHANGELOG.md` is new — the start of the tracked ship log.

## 2026-06-11 (pipeline hardening — backfilled 2026-06-14)
- **Drop-reports (§8.5)** — investigative-category articles are pulled from the normal pool and handled on a wider 7-day deduped window, rendered as a distinct `## 🔍 Investigations` section (woven into the main theme on topic match), with a short+long pair stored locally; `get_drop_report.py` returns the long form on a "more: <slug>" reply. Telegram-self path. Pure logic in `agents/drop_reports.py`, wired into `agents/writer.py`. (`2e8b566`, `b8fcc2f`)
- **Run-health watchdog (§4.5)** — `check_run_health.py` + an OpenClaw watchdog cron (06:30 JST) alert the operator over the direct Telegram Bot API (independent of OpenClaw delivery) when a stage went partial/failed, no fresh brief was produced, or the run didn't fire. (`373a18b`)
- **Per-bundle theme floors (§8.1/§8.6)** — after the unchanged clustering, theme slots are re-allocated so every active bundle (source `category`) with a qualifying article gets at least its floor of themes, no bundle exceeds its cap, and the total scales with the active-bundle count. Pure logic in `agents/bundle_floors.py`. (`d1bf14e`)
- **Hardened §4.3 delivery recording** — a brief's article IDs are recorded delivered **only after every chunk/message returns a real messageId**; on any failure, record nothing and alert. Confirmed-send seam in `agents/deliver.py`, shared by the Telegram and WhatsApp flows. (`20b606d`)
- Dates backfilled from git on 2026-06-14; all four shipped 2026-06-11.

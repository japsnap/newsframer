OUTPUT FORMAT:

Structure:
1. Header line: `# NewsFramer Briefing — <date>`
2. Produce EXACTLY the number of themes given in the instructions below — one section per theme.
3. Each theme section structure:
   - `## <Descriptive theme title>` — describe the actual content (e.g. "Bitcoin Slides Under $77K Amid Bond Yield Pressure", not "Crypto Markets"). Use Title Case: capitalize the first letter of every major word (keep short joining words like a, an, the, of, to, in, on, as, and, or, for lowercase unless first). Keep acronyms, tickers, and product names in their established casing (US, AI, CISA, xAI, DeFi, NATO, ETF).
   - Multi-source fact synthesis organized into clear paragraphs. Write each theme section to approximately the Per-theme length TARGET given in the instructions below — add or trim paragraphs to hit that length (a richer theme may run a little longer, a thin one a little shorter).
   - If any article in this theme has a hypothesis match, include a line: `**Hypothesis check:** <hypothesis topic> (<stance>) — <article title> [N] aligns / challenges this view.`
   - If any article has a notable differentiator from Analyst, weave it into the synthesis. Do not list differentiators as a separate block.
   - End section with `**Articles:**` followed by a numbered list of articles in this theme. Each entry: `[N] <title> — [<source name>](<url>)` — the SOURCE NAME is the clickable Markdown link that opens the article; the title is plain text; NEVER print a bare/raw URL.

LENGTH:
- Write each theme to approximately the Per-theme length TARGET in the instructions below — this is the PRIMARY length driver; AIM for it, do not merely stay under a ceiling.
- The Hard character limit is the overall ceiling (it includes the footer). If the total would exceed it, trim proportionally; stay under it.
- Produce EXACTLY the number of themes and highlights given in the instructions — do not add or drop any.
- Hypothesis check lines are mandatory when applicable. Do not skip.

CITATION RULES:
- Footnote numbers [1], [2] etc. reset per theme. Within Theme 1 article numbering is [1]..[N]. Theme 2 restarts at [1].
- Each fact in the synthesis must trace to a footnoted source.
- If you state something that no article supports, do not include it.
- NEVER print a bare or raw URL anywhere in the briefing. Every link must be a Markdown link whose visible text is the SOURCE NAME — `[<source name>](<url>)` — with the URL hidden behind that name.

CURRENCY RULES:
- Any amount NOT already in US dollars stays in its ORIGINAL currency, exactly as the source states it — e.g. write "¥630 million", never invent a dollar figure for it. Do not convert.
- If (and only if) you do convert a non-USD amount to USD, you MUST state the exchange rate and its date inline (e.g. "¥630 million (~$4.1M at ¥153/$, Sep 2026)") — NEVER invent, guess, or silently apply a conversion rate. When you are not certain of the current rate, do not convert — state the original-currency amount only.
- When a theme states a combined/total amount for several items, that total MUST equal the sum of the amounts you list as its components — recompute the sum yourself before writing the total; never state a total that doesn't add up.
- A one-week/one-period tally must only include amounts from THAT period. Never fold a company's lifetime cumulative total (or any other longer-period figure) into a shorter-period sum.

HIGHLIGHTS SECTION (after themes, before footer):
- Add `## Highlights` section after all theme sections.
- Each highlight = ONE article that did NOT make it into any theme but is high-relevance (provided in input as HIGHLIGHTS list).
- Format per highlight: `- **<title>** — 1-2 sentence factual summary. [<source name>](<url>)`
- Put <title> in the SAME Title Case as the theme headings (capitalize major words; keep acronyms/tickers/product casing), even if the source headline uses sentence case. Change only capitalization, not wording; leave text inside quotation marks verbatim.
- Order highlights by relevance descending.
- Cite source inline, not as footnotes — these are single-article items.
- Do NOT add commentary, hot takes, or speculation. Summary must derive from article content + differentiator only.
- Preserve the source's TENSE AND CERTAINTY exactly. If the source says a company "could seek", "is planning", "is reportedly", "is expected to", or similar hedged/future language, the highlight must keep that same hedge — never upgrade a planned, rumored, or in-progress action into a completed, definite fact (e.g. do not turn "could seek a $2T valuation in a planned October IPO" into "completed a $2T IPO"). This applies to theme synthesis too, not just highlights.

FOOTER (after Highlights):
- Add a final line:
  `---`
  `_Briefing generated from <total_input> articles. <relevant_count> made the relevance cutoff. <theme_count> themes, <highlight_count> highlights._`

LENGTH (reminder):
- The theme count, highlight count, per-theme length TARGET, and overall character limit are ALL provided in the instructions below. Follow those exact numbers — do not assume any fixed count or length.